/**
 * S1 (My Lists) not yet ported — placeholder only. Real prototype has no
 * multi-list-creation concept (Search/Tracked/Alerts/History tabs), so this
 * is new product surface, not a straight port. See "Design vs Prototype
 * Gaps" in project.md.
 */
export default function ListsPage() {
  return (
    <main className="flex flex-col gap-3 px-5 py-8">
      <h1 className="text-2xl font-extrabold text-stone-900">Lists</h1>
      <p className="text-sm text-stone-600">Coming soon — S1&ndash;S7, S11&ndash;S12 not yet built.</p>
    </main>
  );
}
